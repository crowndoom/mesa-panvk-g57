name: release Mesa (Panfrost + PanVK) for Termux (aarch64, native)

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      build_type:
        description: "Build type"
        type: choice
        required: true
        default: both
        options:
          - both
          - debugoptimized
          - release
      release_tag:
        description: "Tag to create (leave empty to auto-generate)"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      date:  ${{ steps.ts.outputs.date }}
      time:  ${{ steps.ts.outputs.time }}
      stamp: ${{ steps.ts.outputs.stamp }}
      tag:   ${{ steps.ts.outputs.tag }}
      types: ${{ steps.ts.outputs.types }}
    steps:
      - name: Compute build timestamp and tag
        id: ts
        run: |
          DATE=$(date -u +'%Y-%m-%d')
          TIME=$(date -u +'%H%M%S')
          STAMP="${DATE}-${TIME}"
          SHORT_SHA="${GITHUB_SHA::7}"
          echo "date=$DATE"           >> "$GITHUB_OUTPUT"
          echo "time=$TIME"           >> "$GITHUB_OUTPUT"
          echo "stamp=$STAMP"         >> "$GITHUB_OUTPUT"
          echo "short_sha=$SHORT_SHA" >> "$GITHUB_OUTPUT"

          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            TAG="${GITHUB_REF_NAME}"
          elif [[ -n "${{ github.event.inputs.release_tag }}" ]]; then
            TAG="${{ github.event.inputs.release_tag }}"
          else
            TAG="build-${DATE}-${TIME}-${SHORT_SHA}"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

          CHOICE="${{ github.event.inputs.build_type }}"
          case "$CHOICE" in
            release)        TYPES='["release"]' ;;
            debugoptimized) TYPES='["debugoptimized"]' ;;
            *)              TYPES='["debugoptimized","release"]' ;;
          esac
          echo "types=$TYPES" >> "$GITHUB_OUTPUT"

  build:
    needs: prepare
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        build-type: ${{ fromJSON(needs.prepare.outputs.types) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify sigbus.patch exists at repo root
        run: |
          set -euo pipefail
          PATCH="${{ github.workspace }}/sigbus.patch"
          if [[ ! -f "$PATCH" ]]; then
            echo "ERROR: sigbus.patch not found at repo root ($PATCH)"
            ls -la "${{ github.workspace }}"
            exit 1
          fi
          echo "Found patch:"
          ls -lh "$PATCH"
          echo "--- first 30 lines ---"
          head -n 30 "$PATCH"

      - name: Create silence header on runner
        run: |
          set -euo pipefail
          HDR="${{ github.workspace }}/panvk_silence.h"
          cat > "$HDR" <<'EOF'
          #pragma once
          #include <stdio.h>
          #include <string.h>
          #include <stdarg.h>

          static inline int __panvk_silence_fprintf(FILE *fp, const char *fmt, ...) {
              if (fp == stderr && fmt && strncmp(fmt, "PANVKDBG", 8) == 0)
                  return 0;
              va_list ap;
              va_start(ap, fmt);
              int r = vfprintf(fp, fmt, ap);
              va_end(ap);
              return r;
          }

          #define fprintf(fp, ...) __panvk_silence_fprintf((fp), __VA_ARGS__)
          EOF
          echo "--- wrote $HDR ---"
          wc -l "$HDR"
          echo "--- contents ---"
          cat "$HDR"

      - name: Build Mesa (${{ matrix.build-type }}) inside Termux container
        run: |
          set -euo pipefail

          mkdir -p "${{ github.workspace }}/termux-out-${{ matrix.build-type }}"
          chmod 777 "${{ github.workspace }}/termux-out-${{ matrix.build-type }}"

          docker run --rm \
            -e BUILD_TYPE="${{ matrix.build-type }}" \
            -v "${{ github.workspace }}/termux-out-${{ matrix.build-type }}:/termux-out" \
            -v "${{ github.workspace }}:/patch:ro" \
            termux/termux-docker:aarch64 \
            bash -c '
              set -euo pipefail
              export TMPDIR="${TMPDIR:-$PREFIX/tmp}"
              echo "=============================================="
              echo "BUILD_TYPE=$BUILD_TYPE (from docker -e)"
              echo "=============================================="

              mkdir -p "$PREFIX/etc/apt/sources.list.d"
              echo "deb https://packages.termux.dev/apt/termux-main stable main" \
                > "$PREFIX/etc/apt/sources.list.d/termux-main.list"

              for i in 1 2 3; do
                if pkg update -y; then break; fi
                echo "pkg update attempt $i failed, retrying in 10s..."
                sleep 10
                if [[ $i -eq 3 ]]; then echo "pkg update failed" >&2; exit 1; fi
              done

              pkg install x11-repo tur-repo -y
              pkg install -y \
                git clang lld binutils cmake autoconf automake libtool make python python-pip ninja llvm bison flex pkg-config \
                fontconfig freetype gnutls libgnutls libgmp \
                libandroid-shmem libandroid-shmem-static libandroid-spawn \
                libx11 xorgproto libdrm libpixman libjpeg-turbo xtrans libxxf86vm xorg-xrandr \
                xorg-font-util xorg-util-macros libxfont2 libxkbfile libpciaccess \
                xcb-util-renderutil xcb-util-image xcb-util-keysyms xcb-util-wm \
                xorg-xkbcomp xkeyboard-config libxdamage libxinerama libxshmfence \
                libxcb libxcomposite libxcursor libxfixes libxrender \
                glslang shaderc vulkan-headers vulkan-loader-generic vulkan-tools \
                libclc spirv-llvm-translator zlib zstd libexpat \
                libxrandr libxrandr-static mesa-demos osmesa glmark2 \
                pulseaudio termux-x11-nightly patch file perl \
                libglvnd-dev libglvnd

              pip install --break-system-packages meson mako pyyaml packaging setuptools

              mkdir -p "$TMPDIR"
              cd "$TMPDIR"
              rm -rf mesa-panvk
              git clone --depth 1 https://github.com/xodiosx/mesa-panvk-g57.git mesa-panvk
              cd mesa-panvk

              if [[ -f /patch/sigbus.patch ]]; then
                echo "Applying /patch/sigbus.patch ..."
                if git apply --verbose /patch/sigbus.patch; then
                  echo ">>> git apply succeeded"
                else
                  echo ">>> git apply failed, falling back to patch(1)"
                  patch -p1 --batch --forward < /patch/sigbus.patch
                fi
                git --no-pager diff --stat || true
              fi

              if [[ ! -f /patch/panvk_silence.h ]]; then
                echo "ERROR: /patch/panvk_silence.h missing"
                exit 1
              fi
              cp /patch/panvk_silence.h "$PWD/src/panvk_silence.h"
              export CFLAGS="-include $PWD/src/panvk_silence.h"
              export CXXFLAGS="-include $PWD/src/panvk_silence.h"

              # ── Patch pan_device.c to fix pan_query_core_count API mismatch ──
              echo "=============================================="
              echo "Patching pan_device.c for new pan_query_core_count signature..."
              echo "=============================================="

              PAN_DEVICE_C="src/gallium/drivers/panfrost/pan_device.c"
              if [[ ! -f "$PAN_DEVICE_C" ]]; then
                echo "ERROR: $PAN_DEVICE_C does not exist!"
                exit 1
              fi
              echo "Found pan_device.c at: $PAN_DEVICE_C"

              perl -0777 -pi -e "s/dev->core_count\s*=\s*pan_query_core_count\(&dev->kmod\.dev->props,\s*&dev->core_id_range\);/dev->core_count = pan_query_core_count(&dev->kmod.dev->props);\n   dev->core_id_range = pan_query_core_id_range(&dev->kmod.dev->props);/g;" "$PAN_DEVICE_C"

              echo "--- Verification of pan_query_core calls: ---"
              grep -n "pan_query_core" "$PAN_DEVICE_C" | head -n 10

              # ================================================================
              # BUILD 1: Vulkan (PanVK) — MUST be built with Gallium DISABLED
              # ================================================================
              echo "=============================================="
              echo "BUILD 1: PanVK (Vulkan) with Gallium disabled..."
              echo "=============================================="

              meson setup build-vulkan \
                -Dbuildtype="$BUILD_TYPE" \
                -Dvulkan-drivers=panfrost \
                -Dgallium-drivers= \
                -Dpanfrost-kmds=kbase,panthor \
                -Dplatforms=x11 \
                -Dglx=disabled \
                -Degl=disabled \
                -Dopengl=false \
                -Dgles1=disabled \
                -Dgles2=disabled \
                -Dbuild-tests=false \
                -Dlibunwind=disabled \
                -Dvalgrind=disabled \
                -Dperfetto=false \
                -Dpanvk-use-kbase=true \
                -Dprefix="$PREFIX" \
                -Dc_link_args=-landroid-shmem \
                -Dcpp_link_args=-landroid-shmem

              ninja -C build-vulkan -j$(nproc) \
                src/panfrost/vulkan/libvulkan_panfrost.so

              # ================================================================
              # BUILD 2: Gallium (OpenGL) — separate meson dir
              # ================================================================
              echo "=============================================="
              echo "BUILD 2: Panfrost Gallium (OpenGL)..."
              echo "=============================================="

              meson setup build-gallium \
                -Dbuildtype="$BUILD_TYPE" \
                -Dvulkan-drivers= \
                -Dgallium-drivers=panfrost \
                -Dpanfrost-kmds=kbase,panthor \
                -Dplatforms=x11 \
                -Dbuild-tests=false \
                -Dlibunwind=disabled \
                -Dvalgrind=disabled \
                -Dperfetto=false \
                -Dprefix="$PREFIX" \
                -Dc_link_args=-landroid-shmem \
                -Dcpp_link_args=-landroid-shmem

              ninja -C build-gallium -j$(nproc)

              # ================================================================
              # Collect outputs
              # ================================================================
              mkdir -p /termux-out/lib
              mkdir -p /termux-out/share/vulkan/icd.d

              cp build-vulkan/src/panfrost/vulkan/libvulkan_panfrost.so \
                 /termux-out/lib/libvulkan_panfrost.so

              GALLIUM_SO=$(find build-gallium -name "libgallium*.so" | head -n1)
              if [[ -z "$GALLIUM_SO" ]]; then
                echo "ERROR: libgallium*.so not found in build-gallium"
                find build-gallium -name "*.so" | head -20
                exit 1
              fi
              echo "Found Gallium driver: $GALLIUM_SO"
              cp "$GALLIUM_SO" /termux-out/lib/libgallium_dri.so

              # ICD manifest using container $PREFIX
              cat > /termux-out/share/vulkan/icd.d/panfrost_icd.aarch64.json <<JSON
              {
                  "ICD": {
                      "api_version": "1.4.354",
                      "library_arch": "64",
                      "library_path": "$PREFIX/lib/libvulkan_panfrost.so"
                  },
                  "file_format_version": "1.0.1"
              }
              JSON

              echo "=============================================="
              echo "Verifying PANVKDBG strings are gone..."
              echo "=============================================="
              for so in /termux-out/lib/*.so; do
                PANVKDBG_STRINGS=$(strings "$so" | grep -c "PANVKDBG" || true)
                echo "$so : PANVKDBG remaining: $PANVKDBG_STRINGS"
              done

              echo "=== /termux-out ==="
              find /termux-out -type f
              echo "=== sizes ==="
              ls -lh /termux-out/lib/
            '

      - name: Package tarball (${{ matrix.build-type }})
        run: |
          set -euo pipefail
          cd "termux-out-${{ matrix.build-type }}"
          TARBALL="mesa-termux-aarch64-${{ matrix.build-type }}-${{ needs.prepare.outputs.stamp }}.tar.gz"
          tar -czvf "../${TARBALL}" .
          ls -lh "../${TARBALL}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mesa-termux-aarch64-${{ matrix.build-type }}-${{ needs.prepare.outputs.stamp }}
          path: |
            termux-out-${{ matrix.build-type }}/
            mesa-termux-aarch64-${{ matrix.build-type }}-${{ needs.prepare.outputs.stamp }}.tar.gz
          if-no-files-found: error

  release:
    needs: [prepare, build]
    runs-on: ubuntu-latest
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List downloaded files
        run: find artifacts -type f

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: "${{ needs.prepare.outputs.tag }} — Mesa (Panfrost + PanVK) Build ${{ needs.prepare.outputs.date }} ${{ needs.prepare.outputs.time }} UTC"
          body: |
            ## Mesa (Panfrost OpenGL + PanVK Vulkan) for Termux (aarch64, native build)

            Built natively inside Termux (`termux/termux-docker:aarch64`)
            on `ubuntu-24.04-arm` — same toolchain class as an on-device
            native Termux build.

            **Build date (UTC):** `${{ needs.prepare.outputs.date }}`
            **Build time (UTC):** `${{ needs.prepare.outputs.time }}`
            **Commit:** `${{ github.sha }}`

            ### Build customizations
            - `sigbus.patch` applied (SHM debug dump SIGBUS fix)
            - `PANVKDBG` debug fprintf calls silenced via a `-include` wrapper
            - **Patched `pan_device.c`:** Fixed `pan_query_core_count` API mismatch
            - **Split builds:** PanVK (Vulkan) is built with Gallium disabled
              (required by the fork's meson.build), while Panfrost (Gallium/OpenGL)
              is built separately. Both outputs are merged.

            ### ⚠️ Runtime Caveat
            PanVK on kbase is **experimental**. Test with `vulkaninfo` and report
            issues upstream to `xodiosx/mesa-panvk-g57`.

            ### Files
            - `mesa-termux-aarch64-*.tar.gz` — full install tree
            - `libgallium_dri.so` — OpenGL driver (Gallium/Panfrost)
            - `libvulkan_panfrost.so` — Vulkan driver (PanVK)
            - `panfrost_icd.aarch64.json` — Vulkan ICD manifest

            ### Install (Termux)
            ```bash
            tar -xzf mesa-termux-aarch64-release-*.tar.gz -C $PREFIX
            export VK_ICD_FILENAMES=$PREFIX/share/vulkan/icd.d/panfrost_icd.aarch64.json
            vulkaninfo --summary
            ```
          files: |
            artifacts/*.tar.gz
            artifacts/**/libgallium_dri.so
            artifacts/**/libvulkan_panfrost.so
            artifacts/**/panfrost_icd.aarch64.json
          overwrite_files: true
          generate_release_notes: true
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
